// Default flutter widget test stub. The auto-scaffolded test from
// `flutter create` referenced a `MyApp` class that doesn't exist in
// this app (our entry widget is BZ5CompanionApp in main.dart). The
// stub previously failed `flutter analyze` with `creation_with_non_type`.
//
// We don't currently maintain Flutter widget tests for this app —
// regression coverage is done via the custom python regression suite
// in this Claude session, the lint.yml workflow, and field testing on
// real BZ5/BZ3 head units. If/when we add real tests they go here.
//
// Keeping the file as an empty stub (rather than deleting) means
// `flutter test` continues to find a test directory and doesn't
// error out, while `flutter analyze` has nothing to complain about.

void main() {
  // Intentionally empty.
}
